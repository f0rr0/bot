{
  "name": "bot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "$(npm bin)/babel-node index.js",
    "lint": "$(npm bin)/eslint --fix ./index.js",
    "prebuild": "npm run lint",
    "build": "$(npm bin)/babel index.js --out-file build.js",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "curryfm": "*",
    "firebase": "*",
    "jsonfile": "*",
    "moment": "*",
    "ramda": "*",
    "spotifier": "*"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-eslint": "^7.0.0",
    "babel-preset-babili": "0.0.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "eslint": "^3.7.1",
    "eslint-config-idiomatic": "^2.1.0"
  }
}
